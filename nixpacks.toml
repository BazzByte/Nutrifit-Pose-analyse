[phases.setup]
nixPkgs = [
  "libGL",
  "glib",
  "gcc-unwrapped",
]

[phases.build]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "sh -c 'uvicorn server:app --host 0.0.0.0 --port $PORT'"
